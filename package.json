{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "scripts": {
    "dev": "cd misskey && pnpm run dev",
    "build": "pnpm --filter misskey run build",
    "start": "cd misskey && pnpm run start",
    "migrateandstart": "cd misskey && pnpm run migrateandstart"
  }
}
